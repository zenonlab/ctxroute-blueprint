[project]
name = "ctxroute-blueprint-crg"
version = "0.0.0"
description = "Reproducible code-review-graph runtime for CTXRoute Blueprint"
requires-python = ">=3.10"
dependencies = [
  "code-review-graph==2.3.8",
]

[tool.uv]
package = false
