{
  "mcpServers": {
    "ctxroute-orchestrator": {
      "type": "stdio",
      "command": "npm",
      "args": ["--prefix", "${CLAUDE_PROJECT_DIR:-.}", "run", "orchestrator:mcp"]
    },
    "code-review-graph": {
      "type": "stdio",
      "command": "npm",
      "args": ["--prefix", "${CLAUDE_PROJECT_DIR:-.}", "run", "crg:mcp"]
    }
  }
}
