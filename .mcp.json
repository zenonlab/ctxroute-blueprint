{
  "mcpServers": {
    "ctxroute-progress": {
      "type": "stdio",
      "command": "npm",
      "args": ["--prefix", "${CLAUDE_PROJECT_DIR:-.}", "run", "progress:mcp"]
    },
    "ctxroute-context-ast": {
      "type": "stdio",
      "command": "npm",
      "args": ["--prefix", "${CLAUDE_PROJECT_DIR:-.}", "run", "context:mcp"]
    }
  }
}
